mode: MainLine
branches:
  main:
    regex: ^master$|^main$
    tag: ''
  feature:
    regex: ^features?[/-]
    tag: useBranchName
  preview:
    regex: ^preview$
    tag: preview
    #mode: ContinuousDeployment
    source-branches: ['main']
ignore:
  sha: []
merge-message-formats: {}