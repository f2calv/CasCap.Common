mode: ContinuousDeployment
branches:
  main:
    regex: ^main$
    label: ''
  feature:
    regex: ^features?[/-]
    label: useBranchName
  preview:
    regex: ^preview?[/-]
    label: preview-{BranchName}
    source-branches: [main]
ignore:
  sha: []
merge-message-formats: {}
